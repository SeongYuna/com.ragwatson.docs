엔티티 오브젝트 펑션으로 분리해 놓은 형태 : 모듈형
연결시키는 방법 API, Connection
라이브러리를 통해 외부에서 만들어진 것을 가져다 쓸 수 있음

레스트풀 : 

cqrs 패턴
명령 (Command) : 시스템(우리의 경우 DB)의 상태가 변하면
쿼리 (Query) : 디비의 상태가 변하지 않으면 (조회)

PK와 FK

BCNF : 결정자이면서 후보키가 아닌 것 제거
4NF : 다치 종속 제거
5NF : 조인 종속성 이용 

교차 엔티티

애자일 방법론

