패턴을 쓰지 않으면 x

객체 = 속성 + 기능 -> 변화, 저장
개체 = 객체 - 기능 -> 저장
함수 = 객체 - 속성 -> 변화

ai 는 객체에 있어야함
ai 는 개체에 넣으면 안됨

전략 패턴 = GoF

캡슐화

init 객체화 객체가 되는 순간 속성과 기능이 생김

이름은 다른데 행위는 똑같아야 함

데이터 베이스 = 저장소

B2B, B2C

함수 -> 객체 -> 개체

            -> 연결 방식 두 가지 : 동기, 비동기

동기 : 응답을 기다림
비동기 :응답 기다리지 않고 바로 다음 업무, 나중에 결과 전달 받음'

데이터가 의미를 가지게 되려면 String으로 짜야 함
맥락을 잡기 위해서
오브젝트에게 스트링의 형태로 주어야 알아들을 수 있음
연결하고자 하는 데이터임을 받아들임

